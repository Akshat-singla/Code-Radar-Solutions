#include <stdio.h>

int main() {
    int num1;
    scanf("%d", &num1);
    if (num1 >=60) {
        printf("Eligible\n");
    } else {
        printf("Not Eligible\n");
    }

    return 0;
}
